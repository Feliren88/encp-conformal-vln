[pycodestyle]
max-line-length = 79
# E203 (whitespace before ":") is not a PEP 8 violation for slices and
# conflicts with black; W503 was inverted by PEP 8 itself (line breaks
# BEFORE binary operators are now the recommendation black follows).
ignore = E203, W503
